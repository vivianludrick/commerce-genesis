import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['in.images.search.yahoo.com', 'rukminim1.flixcart.com','cdn.mos.cms.futurecdn.net'  ],  // Add the domain here
  },
};

export default nextConfig;
